# TaskManager

A simple **Todo App** built with **React** (frontend), **Express.js** (backend), and **PostgreSQL** (database).  
Users can **add**, **view**, and **delete tasks**. Future updates may include timestamps and task editing.

---

## 📂 Project Structure

